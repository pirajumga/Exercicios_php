Escreva uma função chamada soma_array que 
retorna a soma dos elementos de um array.

<?php

$array = [10,10];

function soma_array($array) {
    return $array[0] + $array[1];
}

echo soma_array($array);
